void loop() {
  // put your main code here, to run repeatedly:
  // READ_COMMAND into (command, args)
  // ACK.
  // enum Commands command = MOVE;
  // for (int i = 0; i < 255; i++) {
  //  int args[3] = {254, 254, 1000};
  //  switch (command) {
  //    case MOVE:
  //      int args_len = sizeof(args)/sizeof(int); 
  //      if (args_len == 3) {
  //        move_motors(args);
  //      }
  // }
 // }
}
